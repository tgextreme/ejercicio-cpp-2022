#include <stdio.h>
#include <windows.h>
#include <string.h>

int main(){
    char ecuacion[20]= "2/3x+1/2x=2x";
    int i=0;
    int operando=2;
    int operandoIntermedio=0;
    int numero
   
    for (i = 0; i < (strlen(ecuacion)); i++)
        {
            operandoIntermedio=1;
            if (ecuacion[i] == 'x')
            {
              operandoIntermedio=operando;
                
            }else{


            }
            
        }

}